services:
  test:
    build:
      context: .
      dockerfile: build/docker/Dockerfile.ubuntu
    image: providapt:latest
    container_name: providapt-test
    privileged: true
    volumes:
      - /sys/kernel/btf:/sys/kernel/btf:ro
      - .:/workspace
    working_dir: /workspace
    command: >
      bash -c "
        echo '=== ProvidAPT Docker Test Environment ===' &&
        echo 'OS:' && uname -a &&
        echo '' &&
        echo '=== Go Version ===' && go version &&
        echo '' &&
        echo '=== Building ===' && make build-userspace 2>&1 ||
          echo '[WARN] build-userspace incomplete (eBPF deps may be missing)' &&
        echo '' &&
        echo '=== Running Unit Tests ===' &&
        GOPROXY=https://proxy.golang.org,direct go test -v -count=1 ./internal/engine/pipeline/... ./internal/engine/provenance/... ./internal/engine/query/... ./internal/engine/collector/... ./internal/engine/filter/... ./internal/engine/identity/... ./internal/storage/cache/... ./internal/storage/export/... ./internal/policy/alert/... ./internal/policy/response/... ./internal/policy/adaptive/... ./internal/policy/armor/... ./internal/policy/honeypot/... ./pkg/config/... ./pkg/anonymize/... ./pkg/secure/... ./pkg/hwaccel/... 2>&1 | tail -30 &&
        echo '' &&
        echo '=== Running Additional Unit Tests ===' &&
        GOPROXY=https://proxy.golang.org,direct go test -v -count=1 ./internal/engine/pipeline/... ./internal/engine/provenance/... ./internal/storage/schema/... ./internal/policy/alert/... 2>&1 | tail -20 &&
        echo '' &&
        echo '=== Running Supply-Chain Unit Tests ===' &&
        GOPROXY=https://proxy.golang.org,direct go test -v -count=1 ./internal/stitcher/graphsketch/... ./internal/policy/supplychain/... 2>&1 | tail -20 &&
        echo '' &&
        echo '=== Go Vet ===' &&
        GOPROXY=https://proxy.golang.org,direct go vet ./internal/engine/... 2>&1 | head -10 &&
        echo '' &&
        echo '=== Done ==='
      "

  shell:
    build:
      context: .
      dockerfile: build/docker/Dockerfile.ubuntu
    image: providapt:latest
    container_name: providapt-shell
    privileged: true
    volumes:
      - /sys/kernel/btf:/sys/kernel/btf:ro
      - .:/workspace
    working_dir: /workspace
    stdin_open: true
    tty: true
    command: /bin/bash

  # Production-style local deployment
  providapt:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: providapt
    privileged: true
    pid: host
    network_mode: host
    volumes:
      - /sys:/sys:ro
      - /sys/kernel/btf:/sys/kernel/btf:ro
      - /sys/fs/bpf:/sys/fs/bpf:rw
      - providapt-log:/var/log/providapt
      - ./scripts/docker/providapt.toml:/etc/providapt/providapt.toml:ro
    environment:
      PROVIDAPT_LOG_LEVEL: "info"
      PROVIDAPT_OUTPUT_DIR: "/var/log/providapt"
      PROVIDAPT_API_REST: ":8080"
      PROVIDAPT_CONTROL_PLANE_MODE: "active-passive"
      PROVIDAPT_CONTROL_PLANE_NODE_ID: "docker-control-plane"
      PROVIDAPT_CONTROL_PLANE_STATE_BACKEND: "postgresql://providapt:providapt-change-me@127.0.0.1:5432/providapt?sslmode=disable"
      PROVIDAPT_SECRETS_PROVIDER: "file"
      PROVIDAPT_SECRETS_BASE_DIR: "/run/secrets"
      PROVIDAPT_TLS_ROTATION_CHECK: "24h"
      PROVIDAPT_TLS_ROTATION_RENEW_BEFORE: "720h"
      PROVIDAPT_LICENSE_ACTIVATION_URL: "http://127.0.0.1:19090/v1/activate"
      PROVIDAPT_LICENSE_REVOCATION_URL: "http://127.0.0.1:19090/v1/revocations"
      PROVIDAPT_UPGRADE_MANIFEST_URL: "http://127.0.0.1:19090/v1/releases/latest"
    depends_on:
      postgres:
        condition: service_healthy
      auth-server:
        condition: service_healthy
    restart: unless-stopped
    stop_grace_period: 30s

  postgres:
    image: postgres:16-alpine
    container_name: providapt-postgres
    network_mode: host
    environment:
      POSTGRES_DB: providapt
      POSTGRES_USER: providapt
      POSTGRES_PASSWORD: providapt-change-me
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U providapt -d providapt"]
      interval: 10s
      timeout: 5s
      retries: 12
    restart: unless-stopped

  auth-server:
    build:
      context: .
      dockerfile: build/docker/Dockerfile.authserver
    image: providapt-auth-server:latest
    container_name: providapt-auth-server
    network_mode: host
    environment:
      PROVIDAPT_AUTH_ADDR: ":19090"
      PROVIDAPT_AUTH_ARTIFACT_DIR: "/var/lib/providapt-auth/artifacts"
      PROVIDAPT_AUTH_CUSTOMER: "ProvidAPT Trial"
      PROVIDAPT_AUTH_EDITION: "enterprise"
      PROVIDAPT_AUTH_MAX_AGENTS: "100"
      PROVIDAPT_AUTH_VALID_DAYS: "365"
      PROVIDAPT_AUTH_LICENSE_SIGNING_KEY: "providapt-dev-license-key"
      PROVIDAPT_AUTH_STATE_PATH: "/var/lib/providapt-auth/state/activations.jsonl"
      PROVIDAPT_AUTH_RELEASE_VERSION: "v1.2.3"
      PROVIDAPT_AUTH_UPGRADE_DOWNLOAD_URL: "http://127.0.0.1:19090/artifacts/providapt.tar.gz"
    volumes:
      - providapt-artifacts:/var/lib/providapt-auth/artifacts:ro
      - providapt-auth-state:/var/lib/providapt-auth/state
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:19090/health >/dev/null"]
      interval: 10s
      timeout: 5s
      retries: 12
    restart: unless-stopped

  prometheus:
    image: prom/prometheus:v2.54.1
    container_name: providapt-prometheus
    network_mode: host
    volumes:
      - ./scripts/docker/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus-data:/prometheus
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
      - "--storage.tsdb.path=/prometheus"
    restart: unless-stopped
    profiles:
      - monitoring

  grafana:
    image: grafana/grafana:11.2.0
    container_name: providapt-grafana
    network_mode: host
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
    volumes:
      - grafana-data:/var/lib/grafana
    restart: unless-stopped
    profiles:
      - monitoring

volumes:
  providapt-log:
  providapt-artifacts:
  providapt-auth-state:
  postgres-data:
  prometheus-data:
  grafana-data:

# Golangci-lint configuration
# https://golangci-lint.run/usage/configuration/
#
# Usage:
#   golangci-lint run ./...
#   golangci-lint run --config .golangci.yml ./pkg/...

version: "2"

run:
  timeout: 5m
  issues-exit-code: 1
  tests: true

output:
  formats:
    text:
      path: stdout
      colors: true
      print-issued-lines: true
      print-linter-name: true

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - bodyclose
    - errorlint
    - forcetypeassert
    - misspell
    - nakedret
    - noctx
    - unconvert
    - unparam
  disable:
    - lll  # long lines (too noisy)
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - gocyclo
          - errcheck
          - forcetypeassert
          - noctx
          - errorlint
      - path: pkg/api/proto/
        linters:
          - revive
          - forcetypeassert
          - staticcheck
          - errcheck
      - path: internal/engine/loader/bpf/
        linters:
          - govet
          - staticcheck
      - text: "SA1019"
        linters:
          - staticcheck
      # The AI package intentionally uses British spelling in identifiers
      # (Analyse, AnalysePrompt) and comments (neighbourhood, MITRE).
      - path: internal/engine/ai/
        linters:
          - misspell
  settings:
    errcheck:
      # Single-value type assertions (without _,ok) panic on failure,
      # which is the idiomatic pattern for protobuf dispatch handlers.
      check-type-assertions: false
      # _ = expr is the Go-idiomatic way to explicitly discard errors
      # (e.g. deferred Close, best-effort cleanup). Do not flag it.
      check-blank: false
    misspell:
      locale: US
      ignore-rules:
        - MITRE
        - mitre
    nakedret:
      max-func-lines: 30

formatters:
  enable:
    - gofmt
    - goimports

issues:
  max-issues-per-linter: 50
  max-same-issues: 10
